{
  "name": "farmconnect-ai-worker",
  "version": "0.1.0",
  "private": true,
  "description": "FarmConnect AI — Cloudflare Workers backend with Gemini AI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "itty-router": "^5.0.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.103.2"
  }
}
